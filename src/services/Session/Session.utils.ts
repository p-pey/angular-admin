import { ISession } from './Session.types';

export class SessionUtils {
  static destroySession(session: ISession | null) {
    if (session) {
    }
  }
  static isValidTime(session: ISession | null) {}
}
